import { useState } from 'react';
import PageFrame from '../components/PageFrame';
import DetailPanel from '../components/DetailPanel';
import { portfolioData } from '../data/portfolioData';

export default function Projects() { const [open, setOpen] = useState(false); const project = portfolioData.project; return <PageFrame eyebrow="04 / selected work" title={<>FEATURED <em>PROJECT</em></>} intro="One project at the center. Click to open its full signal." ><div className="project-stage screen-stage"><span className="project-stage-number">PROJECT / 01</span><div className="project-stage-copy"><div className="eyebrow">IoT / monitoring system</div><h2>IoT-Based Water Quality<br /><em>Monitoring System</em></h2><div className="tags">{project.technologies.map((tech) => <span key={tech}>{tech}</span>)}</div><button className="button button-solid" data-cursor="OPEN" onClick={() => setOpen(true)}>EXPLORE PROJECT ↗</button></div><div className="project-stage-visual"><div className="project-screen"><span>WATER_MONITOR / ONLINE</span><strong>97.4 <small>NTU</small></strong><small>TURBIDITY SENSOR</small><div className="wave-line" /></div><div className="project-flow"><i>sensor</i><b>→</b><i>controller</i><b>→</b><i>display</i></div></div></div>{open && <DetailPanel title={project.title} eyebrow="Project detail" onClose={() => setOpen(false)}><p>{project.description}</p><div className="detail-tags">{project.technologies.map((tech) => <span key={tech}>{tech}</span>)}</div></DetailPanel>}</PageFrame>; }
