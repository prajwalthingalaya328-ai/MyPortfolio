import { useState } from 'react';
import PageFrame from '../components/PageFrame';
import DetailPanel from '../components/DetailPanel';
import { portfolioData } from '../data/portfolioData';

export default function Contact() { const [open, setOpen] = useState(false); const [status, setStatus] = useState(''); const submit = (event) => { event.preventDefault(); setStatus('Your message is ready to be connected to an email service.'); event.currentTarget.reset(); }; return <PageFrame eyebrow="09 / contact" title={<>LET'S <em>CONNECT.</em></>} intro="Choose a path. Contact opens a compact message panel." ><div className="contact-stage screen-stage"><a className="contact-choice" data-cursor="CONNECT" href={portfolioData.linkedin} target="_blank" rel="noreferrer"><span>LINKEDIN</span><strong>CONNECT ↗</strong></a><a className="contact-choice" data-cursor="CODE" href={portfolioData.github} target="_blank" rel="noreferrer"><span>GITHUB</span><strong>EXPLORE ↗</strong></a><button className="contact-choice" data-cursor="TALK" onClick={() => setOpen(true)}><span>CONTACT</span><strong>MESSAGE →</strong></button></div>{open && <DetailPanel title="Send a message" eyebrow="Contact detail" onClose={() => setOpen(false)}><form className="compact-form" onSubmit={submit}><label>Name<input name="name" required /></label><label>Email<input name="email" type="email" required /></label><label>Message<textarea name="message" required /></label><button className="button button-solid" type="submit">SEND MESSAGE ↗</button><p role="status">{status}</p></form></DetailPanel>}</PageFrame>; }
